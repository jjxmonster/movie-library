import React from 'react';

import { StyledMainSectionContainer } from './MainSection.css';

const MainSection = ({ children }) => {
   return <StyledMainSectionContainer>{children}</StyledMainSectionContainer>;
};

export default MainSection;
